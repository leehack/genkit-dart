name: schemantic_sample
description: A sample application using 3rd-party serializers with Genkit.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  genkit: ^0.16.1
  genkit_google_genai: ^0.3.1
  json_annotation: ^4.11.0
  schemantic: ^0.2.2

dev_dependencies:
  build_runner: ^2.4.9
  json_serializable: ^6.13.0
  test: ^1.24.0
