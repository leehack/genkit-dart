name: mcp_server_sample
publish_to: none
description: Genkit MCP stdio server testapp
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  genkit: ^0.16.1
  genkit_mcp: ^0.3.1
  schemantic: ^0.2.2

dev_dependencies:
  build_runner: ^2.10.5
  schemantic_builder: ^0.1.2
